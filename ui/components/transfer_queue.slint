export component TransferQueue inherits Rectangle {
    height: 140px;
    background: #20242d;
    VerticalLayout {
        padding: 10px;
        Text { text: "Transfer Queue"; color: #f4f7fb; }
        Text { text: "No active transfers"; color: #a8b3c7; }
    }
}
