export component SftpPanel inherits Rectangle {
    height: 220px;
    background: #252b36;
    VerticalLayout {
        padding: 10px;
        spacing: 6px;
        Text { text: "SFTP"; color: #f4f7fb; }
        Text { text: "Connect a session to browse remote files."; color: #a8b3c7; }
    }
}
