export component TunnelPanel inherits Rectangle {
    height: 180px;
    background: #20242d;
    VerticalLayout {
        padding: 10px;
        Text { text: "Tunnels"; color: #f4f7fb; }
        Text { text: "No active tunnels"; color: #a8b3c7; }
    }
}
