export component QuickCommands inherits Rectangle {
    height: 180px;
    background: #252b36;
    VerticalLayout {
        padding: 10px;
        Text { text: "Quick Commands"; color: #f4f7fb; }
        Text { text: "Command presets will appear here."; color: #a8b3c7; }
    }
}
