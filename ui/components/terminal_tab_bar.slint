export component TerminalTabBar inherits Rectangle {
    height: 36px;
    background: #1b1f27;
    HorizontalLayout {
        padding-left: 8px;
        spacing: 4px;
        Button { text: "Welcome  Disconnected  x"; }
        Button { text: "+"; }
    }
}
